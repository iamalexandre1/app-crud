export interface taskI {
  id: string;
  taskTitle: string;
  taskDescription: string;
  taskIsCompleted: boolean;
}

export interface taskFormI {
  taskTitle: string;
  taskDescription: string;
  taskIsCompleted: boolean;
}
